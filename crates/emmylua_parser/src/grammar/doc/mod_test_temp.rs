
#[cfg(test)]
mod test {
    use crate::{LuaDocParser, LuaDocLexer};

    #[test]
    fn test_parse_instance_flag_strict() {
        // This should pass if strict parsing is intended
        // But we want to see if ( instance ) fails
    }
}
